Manage Users

<%= @users.inspect %>
<%= paginate @users %>

<%= form_with url: admin_users_path, method: :get do |form| %>
  <div class=''>
    <%= text_field_tag 'first_name', params[:first_name], placeholder: 'First Name', class: '' %>
    <%= text_field_tag 'last_name', params[:last_name], placeholder: 'Last Name', class: ''  %>
    <%= text_field_tag 'email', params[:email], placeholder: 'Email', class: ''  %>
    <%= form.submit 'Search', class: '' %>
  </div>
<% end %>

<table class=''>
  <thead>
    <tr>
      <th>ID</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th>Role</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.email %></td>
        <td><%= user.role %></td>
        <td style='width:15%'>
          <span>
          <button class=''>
            View
          </button>
          <button class=''>
            Edit
          </button>
          <button class=''>
            Delete
          </button>
          </span>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>